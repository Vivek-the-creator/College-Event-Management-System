import { Shell } from '@/components/layout/shell';

export default function VendorPage() {
  return (
    <Shell>
      <h1 className="text-2xl font-semibold">Vendor Panel</h1>
      <p className="mt-2 max-w-2xl text-sm text-slate-600">Store operations, orders, payouts, and analytics.</p>
    </Shell>
  );
}
